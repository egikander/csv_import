For the first deploy, use the **`npm run deploy`** command in project dir

Next time use the **`npm start`** command

The running application available by the following link [http://localhost:3000](http://localhost:3000)
